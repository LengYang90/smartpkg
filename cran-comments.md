## Test environments

- Local macOS Tahoe 26.3.1, R 4.6.0

## R CMD check results

0 errors | 0 warnings | 0 notes

Local check command:

```sh
_R_CHECK_CRAN_INCOMING_=false _R_CHECK_SYSTEM_CLOCK_=false R CMD check --as-cran --no-manual smartpkg_0.1.0.tar.gz
```

The local environment does not have `pdflatex` installed, so the manual PDF
check must be run in a LaTeX-enabled environment before submission.

## Submission notes

This is a new submission.
